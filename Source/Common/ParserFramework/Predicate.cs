
namespace Common.ParserFramework
{
    /// <summary>
    /// A predicate for the reduction process.
    /// </summary>
    /// <param name="item">The object to qualify.</param>
    /// <returns>True if the object matches the predicate.</returns>
    public delegate bool Predicate(object item);
}
